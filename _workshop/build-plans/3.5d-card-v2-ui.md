# 3.5d - Card v2 UI (rearrange + full-screen open mode)

**Status:** 🅿️ PARKED → moved to **Section 5 (5A - UI pass)**. **Owner:** open / unassigned.
**Builds on:** 3.5a (create), 3.5b (edit), 3d (gate).

> **2026-06-11 decision (Ayush): 3.5 is CLOSED here. The UI work does NOT happen as 3.5d.**
> Reason: Sella (Section 4) is next, and Sella will reshape the chat + card UI. Restyling now means
> restyling again after Sella lands. So we **research Sella first (4.0)**, then do the UI **in parallel**
> with the Sella build, as a fresh **Section 5 (5A)**. The full UI scope (card open mode + layout, chat
> heading, the message typing bar, the left chat/relationship nav minimised to icons, and the Sella chat UI)
> now lives in `5a-ui-pass.md`. The notes below are kept as the seed thinking for 5A.
>
> A throwaway open-mode overlay (blurred backdrop, centered vertical card) was prototyped live this session
> and then **reverted (not kept)** - Ayush wants the UI re-approached fresh in 5A after Sella research.

---

> 3.5d is the LAST piece of "card v2" - the visual pass, saved for last on purpose so the card is restyled
> ONCE, now that create + edit have shown us every field the card must hold. It is pure presentation: no new
> DB writes, lowest risk. Two jobs: (1) **rearrange** the card so the information reads well, and (2) a
> **full-screen "blank background, only the card" open mode** (today the card floats on the right of the chat
> via `DealPin`, which crowds the chat at narrow widths).

## What we now know the card must show (from 3a-3.5b)

HS number band · the confirm gate (`ConfirmBar`, top) · doc term + date · seller/buyer boxes · value net +
gross · the private "only you" box · delivery · the products list · the **Edit** button (3.5b) · and the back
(Signals + Logs/version history). A created/edited card can also be **price-less** ("Price TBD") and carries a
**version** (so a "v2" cue + the latest change note have a natural home now).

## Open decisions to settle with Ayush FIRST (the earlier picker was dismissed)

1. **Open mode / position** - full-screen overlay (blank backdrop, only the card) · centered modal over a
   dimmed chat · or keep the floating side panel + an expand button. *(Ayush leaned toward "the location is
   perfect where it is now" for the in-chat pin - so the full-screen mode is likely an ADDITIONAL open mode,
   not a replacement. Confirm.)*
2. **Card structure** - single scrolling card (sections) · tabs (Deal / Activity / Signals) · or keep today's
   flip (front facts / back signals+logs).
3. **Where activity/version history lives** in the new layout (the change notes are now first-class).
4. **Editing** - already decided in 3.5b: a SEPARATE prefilled form (not in-place). Keep.

## Rough shape (refine after the decisions)

- **Phase 0 - decisions + a throwaway sketch** (consider the `prototype` skill for 2-3 layout variations).
- **Phase 1 - full-screen open mode** (a route or an overlay that shows only the card on a blank backdrop;
  reuse the existing `DealCard`). Keep the in-chat pin as-is per Ayush.
- **Phase 2 - rearrange `CardFront`** (and back) for a cleaner hierarchy; surface the version + latest note.
- **Phase 3 - verify live both sides; tsc/eslint clean; wrap.**

## Boundaries

- No data changes (read-only presentation). No new server actions. Discount / CC / Sella still parked.
