# G4 · T08 — ops housekeeping the ADR surfaced

**Status:** built · verified · **awaiting Muskan on 8 deviations.** The slug's LAST ticket.
Evidence: `PLAN-T08.md` rev 3 · `blocked.md` · commits `466cfc2` (the migration fix) + `fcf223f`.

> **A doc-only XS ticket found a production security regression in the ship batch.** That is the
> headline. Everything else here is the ledger being made executable.

## What it closes

`docs/deploy/cloud-migrations-pending.md` is the document an ops push against **production** is
driven from. It said *"push the slug as one batch, in timestamp order"* and **was unexecutable from
itself.** Now it is.

| before | after |
|---|---|
| `## PENDING (local only — NOT on cloud yet)` holding **13** subsections, every one applied | `## ✅ HISTORICAL`, each of the 13 ruled and its push recorded |
| non-migration deploy debt buried inside "applied" batches | its own `## ⚠️ OUTSTANDING — NON-MIGRATION DEPLOY DEBT` heading |
| a section shouting **"PRODUCTION IS STILL VULNERABLE"** about a fix applied in session 64 | struck (not deleted), marked `✅ APPLIED 2026-07-16` |
| **two** sections advertising runnable push procedures, one stale | both bannered `🔴 SUPERSEDED` |
| **3 of 6** migrations in the ship batch had no entry | all six ledgered, in timestamp order |

**Acceptance rule verified independently, not asserted:** exactly one section claims un-pushed
migrations (`:26`, six entries) · non-migration debt is under a heading that says so (`:435`) · no
unbannered push procedure remains (`:778`, `:854`).

## Gate evidence
`supabase db reset` clean · **39/39 SQL runners over 44 suite files** (5 never execute:
`announcement_projection_test`, `change_reason_log_test`, `onboard_company_categories_test`,
`pending_change_lock_test`, `rls_isolation_test` = DEV-161). **Never "all".** No migration, no
source, no schema touched by T08 itself.

---

# ▶ RULE ON THESE — 8 deviations

**1–5 are the plan's own (§4b); 6–8 the builder declared.**

### 1. The missing ledger entries were carried by T08, which its written criteria never mention
Sourced from four places (`STATE.md`, `G4-T06.md:228` — *"**T08 owns this.**"*, `G4-T07.md:114`,
`REVIEW.md:350`). **L-017 widening.**

### 2. `20260607090000` belongs to no ticket at all
Not slug work — unledgered work *discovered* by this slug. Ledgering it is a further widening.

### 3. `:402`, `:264`, and both push-procedure banners are outside both written criteria
Same file, adjacent rot. Fixing `:402` was not optional in practice: it claimed a **live production
vulnerability** that was closed five weeks ago.

### 4. ADR-0005 was edited — a third file
`:875` credited `anon_execute_lockdown_test.sql` with a relation check it **does not contain** (0
matches); the real enforcers are `cross_tenant_lockdown_test.sql:86` and
`pricelist_item_tier_test.sql:102`. `:863` repeated the ticket's stale citation. **Added to `Files`
and fence-checked at rev 3 rather than done quietly** — rev 2 had edited it with the file in
neither `Files`, the deviations table, nor the fence grep, violating **L-032 and L-017, both cited
by name in the same plan.**

### 5. ✅ ALREADY RULED — a migration was amended, from a doc-only ticket
`20260607090000` granted `execute on functions` to **`anon`**; `20260817120000` §3 revokes exactly
that and has been **live on prod since 2026-08-17**. Correct locally **only because the file replays
first** — it is named 2026-06-07 and was **authored 2026-08-22**. On a cloud push it lands **last**
and re-widens the default with nothing to narrow it. Muskan ruled **amend in place** (`466cfc2`);
replay end state **byte-identical**, now order-independent. **L-034.**

### 6. The acceptance rule FAILED on first pass — nine more stale claims, none in the plan
The builder **ran** the rule instead of assuming it. Nine survivors inside the reconciled section,
including **two `####` headings still ending "— NOT on cloud"** (Phases 11, 12), a Phase-10 row
reading *"Deferred — Push when it ships"*, three Phase-7 siblings called *"still local-only"* that
are all live, and Phase 13's *"Push AFTER the still-pending Phase 10 + 11 + 12"*.
**Why it matters beyond this file: a reader who jumps to a heading never sees the section banner.**
Fixed by annotating in place, nothing deleted — but it is scope beyond §4's step list.

### 7. The file's `Purpose:` block was rewritten
Given the two-classes distinction and the rule *"annotate in place, cite by heading not line"*.
Not in the plan — it is the header the whole ticket turned on.

### 8. One word changed inside a verbatim move
`erase-expired-accounts`'s bullet said its cron `net.http_post` was *"above"*, which stopped
resolving once the block moved. Now names `20260706090200_erasure_cron.sql`. Disclosed in-file.
Everything else moved verbatim.

---

## Owed before `/ship` (not gate items)
- **Phase 11's `invite.html` is kept as OWED, not discharged.** `SUPABASE_SECRET_KEY` is discharged
  (the 2026-06-23 entry names it). The same entry says *"3 email templates"* and **never names
  which** — nothing in the repo does. Ruled explicitly rather than swept.
- **The ship batch is SIX migrations and needs `--include-all`** — `20260607090000` sorts ~14 months
  before cloud's tip. Its pre-flight reads the `defaclacl` **grantee array element by element**
  (`anon=X/postgres` absent on `objtype='f'`, present on `'r'`/`'S'`) plus the event-trigger check.
  **A letters-only check is what hid L-034.**
- **S6 + S8 still owed** from T07 — `supabase db diff --linked`, `get_advisors` — cloud-only.
- **T07's four unruled G4 items** remain open (the L-017 widening, the seed-pollution class, the
  test-file fence, the pill skipping G2).
