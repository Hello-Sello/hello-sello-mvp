# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/

# Temp / backup files
*.tmp
*.bak
*.orig

# Logs
*.log

# Personal agent config — each engineer keeps their own, never committed
CLAUDE.md

# Local environment / secrets (defensive — no secrets expected in design docs)
.env
.env.local

# Claude Code internal
.claude/worktrees/

# Product-thinking workspace - now shared with the team (build-plans, pov, notes, prototypes)
# (was gitignored; tracked from 2026-06-10 so the team can reuse this arrangement)

# Visual companion (brainstorming skill) - mockups persist here per session
.superpowers/

# --- Next.js / Node (app) ---
/node_modules
/.next/
/out/
/build
/coverage
*.tsbuildinfo
next-env.d.ts
.vercel
*.pem
npm-debug.log*
.obsidian/
